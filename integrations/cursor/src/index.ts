// Public API of @devcortex/cursor.
//
// The installer (`installCursor`) plus the deterministic Cursor rule (.mdc) /
// MCP builders it is composed from. Surfaces (the `devcortex install cursor`
// CLI command, tests, other tooling) consume these directly.

export * from './templates';
export * from './install';
